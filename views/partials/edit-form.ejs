    <!-- edit-form.ejs -->
    <div class="tdl-edit-form" id="edit-form-<%= note._id %>">
        <div class="tdl-edit-form-inner">
          <form action="/notes/<%= note._id %>?_method=PUT" method="POST">
            <input type="text" name="item" value="<%= note.item%>">
            <input type="submit" value="Save" />
          </form>
        </div>
      </div>